<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener Dashboard</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />
    <style>
      body {
        background: linear-gradient(135deg, #f8fafc 0%, #cfd9df 100%);
        min-height: 100vh;
        margin: 0;
        font-family: "Roboto", sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      .main-heading {
        margin-top: 2.5rem;
        font-size: 2.6rem;
        font-weight: 900;
        color: #3366cc;
        letter-spacing: 2px;
        text-shadow: 0 2px 14px #e3e7ed99;
      }
      .container {
        background: #fff;
        margin-top: 2.8rem;
        padding: 2.7rem 2.3rem 1.2rem 2.3rem;
        border-radius: 18px;
        box-shadow: 0 6px 28px rgba(80, 121, 174, 0.12);
        width: 100%;
        max-width: 420px;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-bottom: 16px;
      }
      label {
        font-size: 1.1rem;
        color: #455;
        margin-bottom: 0.25rem;
      }
      input[type="text"] {
        padding: 0.8rem 1.1rem;
        border: 1.5px solid #9dc3e6;
        border-radius: 8px;
        font-size: 1.05rem;
        outline: none;
        transition: border 0.2s;
      }
      input[type="text"]:focus {
        border-color: #3366cc;
      }
      button,
      .btn-action {
        background: linear-gradient(90deg, #74ebd5 0%, #5b76e9 100%);
        color: #fff;
        border: none;
        border-radius: 8px;
        padding: 0.55rem 1.2rem;
        font-size: 1rem;
        font-weight: 700;
        cursor: pointer;
        transition: background 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 6px rgba(91, 118, 233, 0.12);
        display: inline-flex;
        gap: 0.35rem;
        align-items: center;
      }
      button:hover,
      .btn-action:hover {
        background: linear-gradient(90deg, #5b76e9 0%, #74ebd5 100%);
        box-shadow: 0 4px 12px rgba(91, 118, 233, 0.18);
      }
      .table-header-row th {
        background: linear-gradient(90deg, #3366cc 0%, #69a3f8 100%);
        color: #fff;
        border: none;
        font-size: 1.05rem;
        font-weight: bold;
      }
      .table-container {
        margin-top: 2rem;
        width: 100%;
        max-width: 820px;
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 6px 24px rgba(80, 121, 174, 0.13);
        padding: 2.1rem 1.2rem 1.2rem 1.2rem;
      }
      .logout-btn {
        position: absolute;
        top: 20px;
        right: 20px;
        background: linear-gradient(90deg, #6c757d 0%, #495057 100%);
        color: #fff;
        border: none;
        border-radius: 8px;
        padding: 0.5rem 1rem;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 6px rgba(73, 80, 87, 0.12);
        display: inline-flex;
        gap: 0.35rem;
        align-items: center;
      }
      .logout-btn:hover {
        background: linear-gradient(90deg, #495057 0%, #6c757d 100%);
        box-shadow: 0 4px 12px rgba(73, 80, 87, 0.18);
      }
      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 1rem;
        background: #fff;
        border-radius: 12px;
        overflow: hidden;
      }
      th,
      td {
        padding: 0.7rem 1rem;
        text-align: left;
        vertical-align: middle;
      }
      tr {
        border-radius: 8px;
        box-shadow: none;
        transition: background 0.17s;
      }
      tr:nth-child(even) {
        background: #f8fafc;
      }
      tr:hover {
        background: #e8f1fa;
      }
      .action-group {
        display: flex;
        align-items: center;
        gap: 6px;
      }
      .btn-delete {
        background: linear-gradient(90deg, #ff7675 0%, #fd5e53 100%);
        color: #fff;
        border-radius: 7px;
        padding: 0.3rem 1.1rem;
        font-size: 0.96rem;
        font-weight: 700;
        border: none;
        margin-left: 0.2rem;
        box-shadow: 0 1px 4px rgba(255, 118, 117, 0.08);
        transition: background 0.2s, box-shadow 0.2s;
        display: inline-flex;
        gap: 0.32rem;
        align-items: center;
      }
      .btn-delete:hover {
        background: linear-gradient(90deg, #fd5e53 0%, #ff7675 100%);
        box-shadow: 0 4px 8px rgba(253, 94, 83, 0.13);
      }
      @media (max-width: 900px) {
        .main-heading {
          font-size: 2rem;
        }
        .container,
        .table-container {
          max-width: 99vw;
        }
      }
      @media (max-width: 650px) {
        th,
        td {
          font-size: 0.96rem;
          padding: 0.53rem 0.5rem;
        }
        .container {
          padding: 1.3rem 0.4rem 0.7rem 0.4rem;
        }
        .table-container {
          padding: 1.1rem 0.2rem 0.7rem 0.2rem;
        }
      }
    </style>
  </head>
  <body>
    <a href="/user/logout" class="logout-btn">
      <i class="bi bi-box-arrow-right"></i> Logout
    </a>
    <div class="main-heading">URL Shortener Dashboard</div>
    <% if (locals.Id) { %>
    <div class="url-generated">
      URL Generated:
      <a href="http://localhost:8001/url/<%= Id %>" target="_blank"
        >http://localhost:8001/url/<%= Id %></a
      >
    </div>
    <% } %>
    <div class="container">
      <form method="post" action="/url">
        <label>Enter Your Original URL:</label>
        <input
          type="text"
          name="url"
          placeholder="https://example.com"
          required
        />
        <button type="submit">
          <i class="bi bi-plus-circle" style="font-size: 1.1em"></i>
          Generate
        </button>
      </form>
    </div>
    <div class="table-container">
      <% if (locals.urls) { %>
      <table>
        <thead>
          <tr class="table-header-row">
            <th>S.No</th>
            <th>ShortID</th>
            <th>Redirect</th>
            <th>Clicks & Action</th>
          </tr>
        </thead>
        <tbody>
          <% urls.forEach((url, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td>
              <a href="/url/<%= url.shortId %>" target="_blank"
                ><%= url.shortId %></a
              >
            </td>
            <td>
              <% if(url.redirectURL.length > 60){ %>
              <span title="<%= url.redirectURL %>"
                ><%= url.redirectURL.substring(0,58) %>...</span
              >
              <% }else{ %> <%= url.redirectURL %> <% } %>
            </td>
            <td>
              <div class="action-group">
                <span
                  style="
                    margin-right: 5px;
                    font-weight: 600;
                    color: #222;
                    font-size: 1.05em;
                  "
                >
                  <%= url.visitHistory.length %>
                </span>
                <form
                  action="/url/<%= url.shortId %>/clear-history"
                  method="POST"
                  style="display: inline"
                >
                  <button
                    type="submit"
                    class="btn-delete"
                    title="Delete history"
                    onclick="return confirm('Delete visit history for this URL?')"
                  >
                    <i class="bi bi-trash"></i> Delete
                  </button>
                </form>
              </div>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } else { %>
      <p style="text-align: center; color: #888">No URLs found.</p>
      <% } %>
    </div>
  </body>
</html>
